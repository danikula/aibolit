apply plugin: 'java'
apply plugin: 'eclipse'

version = buildVersion

dependencies {
    compile files(projectLibsDir.path + '/android4.jar')
}

compileJava {
    doFirst {
        rootProject.tasks['downloadLibs'].execute()
    }
}