<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.danikula.aibolit.test"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="4" />

    <instrumentation
        android:name="android.test.InstrumentationTestRunner"
        android:targetPackage="com.danikula.aibolit.test" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:name=".AibolitTestApplication">
        <activity
            android:label="@string/app_name"
            android:name=".AibolitChatActivity" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ClassicChatActivity" />
        <activity android:name=".TestInjectActivity" />

        <uses-library android:name="android.test.runner" />
        
    </application>

</manifest>